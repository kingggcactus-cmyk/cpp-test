#include <iostream>
#include <cstring>
using namespace std;

int main()
{
    string str[20];
    cin >> str[];
    int count;
    for(int i = 0; i < str; i++)
    {
        if(str[i]==str[i+1]) count++;
    }
    return 0;

}